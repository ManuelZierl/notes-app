{
  "name": "@ma-zierl/notes-app",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "description": "Standalone Notes app for Kestral.",
  "scripts": {
    "build": "node build.mjs",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "svelte": "^5.0.0",
    "typescript": "^5.7.0"
  }
}
